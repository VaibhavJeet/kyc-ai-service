name: trustvault_shield
description: TrustVault Shield - Protect yourself from scams and fraud
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  shared_preferences: ^2.2.0
  flutter_local_notifications: ^16.0.0
  permission_handler: ^11.0.0
  contacts_service: ^0.6.3
  call_log: ^4.0.0
  url_launcher: ^6.2.0
  share_plus: ^7.2.0
  provider: ^6.1.0
  flutter_secure_storage: ^9.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
